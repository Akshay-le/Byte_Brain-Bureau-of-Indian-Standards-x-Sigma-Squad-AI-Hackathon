# BIS Standards RAG Engine – Dependencies
# Core (no GPU required)
flask>=3.0.0
anthropic>=0.40.0

# PDF ingestion (optional – only needed to re-ingest BIS SP-21 PDF)
pdfplumber>=0.10.0

# Utilities
pathlib2>=2.3.7; python_version < "3.4"
